#include<obliv.oh>
#include"testStack.h"
#include<stdio.h>
#include"ocStack.oh"

void testStack(void* args)
{
    protocolIO *io = args;
    obliv bool b;
	
	ocStack* os = ocStackNew(15, 1);
	fprintf(stderr, "Stack Created\n");
	ocStackRelease(os);
	fprintf(stderr, "Stack Released\n");
	
}
